<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheet/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>

  <title>Document</title>
</head>

<body>
  <div class="w-full h-screen bg-zinc-900 p-10 text-white">
    <div class="flex justify-end mb-2">
        <a class="bg-red-600 rounded-full inline-block text-sm px-3 py-2 tracking-tight" href="/logout">LogOut</a>
    </div>
    <hr class="mt-4">
    <div class="w-full bg-zinc-900 p-10 text-white">
        <h5 class="mb-5">Edit your post.</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea class="p-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-lg" placeholder="What's on your mind?" name="content"><%= post.content %></textarea>
            <input class="px-3 py-2 bg-yellow-600 text-black block mt-2 rounded-md w-32 text-sm cursor-pointer" type="submit" value="Update Post">
        </form>
    </div>
  </div>
</body>

</html>