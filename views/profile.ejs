<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheet/style.css" />
  <script src="https://cdn.tailwindcss.com"></script>

  <title>Document</title>
</head>

<body>
  <div class="w-full h-screen bg-zinc-900 p-10 text-white">
    <div class="flex justify-between mb-2">
      <a class="bg-blue-600 rounded-full inline-block text-sm px-3 py-2" href="/test">Test</a>
      <a class="bg-red-600 rounded-full inline-block text-sm px-3 py-2 tracking-tight" href="/logout">LogOut</a>
    </div>
    <hr class="mt-4">
    <div class="w-full bg-zinc-900 p-10 text-white">
      <div class="flex items-start gap-4">
        <div class="w-10 h-10 rounded-md overflow-hidden">
          <img class="w-full h-full object-cover" src="/images/upload/<%= user.profilePic %>" alt="">
        </div>
        <h3 class="text-3xl mb-1">
          <span class="text-light">Hello</span>, <%= user.name %> 👋 !
        </h3>
      </div>
      <h5 class="mb-5 mt-3">You can create a new post.</h5>
      <form method="post" action="/post">
        <textarea class="p-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-lg" placeholder="What's on your mind?" name="content"></textarea>
        <input class="px-3 py-2 bg-blue-600 block mt-2 rounded-md w-40 text-sm cursor-pointer" type="submit" value="Create New Post">
      </form>
      <div class=" posts mt-20">
        <h3 class="text-zinc-400">Your Posts</h3>
         <div class="postcontainer mt-4">
          <% user.posts.reverse().forEach((post) => { %>
            <div class="post w-1/3 p-4 border-[1px] mb-5 border-zinc-700 rounded-md bg-zinc-800 ">
              <h4 class="text-blue-500 mb-1">@<%= user.username %></h4>
              <p class="text-sm tracking-tight"><%= post.content %></p>
              <small class="inline-block mt-3 text-sm"><%= post.likes.length %> Likes</small>
              <div class="flex">
                <a class="text-blue-500 mr-4" href="/like/<%= post._id %>"> <%= post.likes.indexOf(user._id) ? "🤍" : "❤️" %></a>
                <a class="text-zinc-500" href="/edit/<%= post._id %>">Edit</a>
                <a class="text-red-600 ml-auto" href="/delete/<%= post._id %>">❌</a>
              </div>
            </div>
          <% }); %> 
        </div>
      </div>
    </div>
  </div>
</body>

</html>